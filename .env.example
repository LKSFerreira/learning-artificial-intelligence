# Copie para `.env` e preencha. O arquivo `.env` não deve ser commitado.
#
# Chave da API Google Gemini (tutor opcional na plataforma).
# Sem esta variável, o app roda normalmente; o tutor retorna mensagem de configuração.
# Documentação: https://ai.google.dev/
GEMINI_API_KEY=
